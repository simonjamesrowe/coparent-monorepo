{
  "scripts": {
    "setup": "npm install -g pnpm@8.12.0 && pnpm install && pnpm --filter @coparent/shared-types build && cp apps/api/.env.example apps/api/.env 2>/dev/null || true && cp apps/ui/.env.example apps/ui/.env 2>/dev/null || true",
    "run": "docker-compose -f docker/docker-compose.yml up"
  }
}
